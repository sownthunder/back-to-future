using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FluxCapacitor : MonoBehaviour
{

	// Used to determine if player gets UI 
	// option to enter vehicle or  not
	public bool withinVehicleRange = false;

	public bool insideDeLeorean = false;

	public bool insideManureTruck = false;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
